// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
)

// ServiceStunnelServerPsk manages a VyOS ServiceStunnelServerPsk configuration.
type ServiceStunnelServerPsk struct{}

// ServiceStunnelServerPskArgs defines the inputs for the ServiceStunnelServerPsk resource.
type ServiceStunnelServerPskArgs struct {
	ServerName string  `pulumi:"serverName"`
	Name       string  `pulumi:"name"`
	IDValue    *string `pulumi:"idValue,optional"`
	Secret     *string `pulumi:"secret,optional"`
}

// ServiceStunnelServerPskState defines the outputs for the ServiceStunnelServerPsk resource.
type ServiceStunnelServerPskState struct {
	ServiceStunnelServerPskArgs
}

// Annotate provides schema metadata for the resource.
func (*ServiceStunnelServerPsk) Annotate(a infer.Annotator) {
	a.Describe(new(ServiceStunnelServerPsk), "Manages a VyOS Pre-shared key name.")
}

// Annotate provides schema metadata for the inputs.
func (args *ServiceStunnelServerPskArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.ServerName, "Parent tag node key")
	a.Describe(&args.Name, "Pre-shared key name")
	a.Describe(&args.IDValue, "ID for authentication")
	a.Describe(&args.Secret, "pre-shared secret key")
}

func serviceStunnelServerPskBasePath(serverName, name string) []any {
	return []any{"service", "stunnel", "server", serverName, "psk", name}
}

// Create sets up the ServiceStunnelServerPsk on VyOS.
func (ServiceStunnelServerPsk) Create(
	ctx context.Context,
	req infer.CreateRequest[ServiceStunnelServerPskArgs],
) (infer.CreateResponse[ServiceStunnelServerPskState], error) {
	if req.DryRun {
		return infer.CreateResponse[ServiceStunnelServerPskState]{
			ID:     req.Inputs.Name,
			Output: ServiceStunnelServerPskState{ServiceStunnelServerPskArgs: req.Inputs},
		}, nil
	}

	ops := buildServiceStunnelServerPskOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[ServiceStunnelServerPskState]{},
			fmt.Errorf("configure ServiceStunnelServerPsk %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[ServiceStunnelServerPskState]{
		ID:     req.Inputs.Name,
		Output: ServiceStunnelServerPskState{ServiceStunnelServerPskArgs: req.Inputs},
	}, nil
}

// Read fetches the current ServiceStunnelServerPsk config from VyOS.
func (ServiceStunnelServerPsk) Read(
	ctx context.Context,
	req infer.ReadRequest[ServiceStunnelServerPskArgs, ServiceStunnelServerPskState],
) (infer.ReadResponse[ServiceStunnelServerPskArgs, ServiceStunnelServerPskState], error) {
	client := getClient(ctx)
	base := serviceStunnelServerPskBasePath(req.State.ServerName, req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := ServiceStunnelServerPskArgs{ServerName: req.State.ServerName, Name: req.State.Name}
			state := ServiceStunnelServerPskState{ServiceStunnelServerPskArgs: args}
			return infer.ReadResponse[ServiceStunnelServerPskArgs, ServiceStunnelServerPskState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[ServiceStunnelServerPskArgs, ServiceStunnelServerPskState]{},
			fmt.Errorf("read ServiceStunnelServerPsk %s: %w", req.State.Name, err)
	}

	args, err := parseServiceStunnelServerPskConfig(req.State.ServerName, req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[ServiceStunnelServerPskArgs, ServiceStunnelServerPskState]{},
			fmt.Errorf("parse ServiceStunnelServerPsk %s config: %w", req.State.Name, err)
	}

	state := ServiceStunnelServerPskState{ServiceStunnelServerPskArgs: args}
	return infer.ReadResponse[ServiceStunnelServerPskArgs, ServiceStunnelServerPskState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing ServiceStunnelServerPsk configuration.
func (ServiceStunnelServerPsk) Update(
	ctx context.Context,
	req infer.UpdateRequest[ServiceStunnelServerPskArgs, ServiceStunnelServerPskState],
) (infer.UpdateResponse[ServiceStunnelServerPskState], error) {
	if req.DryRun {
		return infer.UpdateResponse[ServiceStunnelServerPskState]{
			Output: ServiceStunnelServerPskState{ServiceStunnelServerPskArgs: req.Inputs},
		}, nil
	}

	ops := buildServiceStunnelServerPskUpdateOps(req.State.ServiceStunnelServerPskArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[ServiceStunnelServerPskState]{},
				fmt.Errorf("update ServiceStunnelServerPsk %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[ServiceStunnelServerPskState]{
		Output: ServiceStunnelServerPskState{ServiceStunnelServerPskArgs: req.Inputs},
	}, nil
}

// Delete removes the ServiceStunnelServerPsk configuration from VyOS.
func (ServiceStunnelServerPsk) Delete(
	ctx context.Context,
	req infer.DeleteRequest[ServiceStunnelServerPskState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := serviceStunnelServerPskBasePath(req.State.ServerName, req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete ServiceStunnelServerPsk %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildServiceStunnelServerPskOps builds set operations for all non-nil fields.
func buildServiceStunnelServerPskOps(args ServiceStunnelServerPskArgs) []vyosclient.Operation {
	base := serviceStunnelServerPskBasePath(args.ServerName, args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.IDValue != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "id"), Value: *args.IDValue})
	}

	if args.Secret != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "secret"), Value: *args.Secret})
	}

	return ops
}

// buildServiceStunnelServerPskUpdateOps computes the diff between previous and current state.
func buildServiceStunnelServerPskUpdateOps(prev, cur ServiceStunnelServerPskArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := serviceStunnelServerPskBasePath(cur.ServerName, cur.Name)

	ops = append(ops, diffStringField(base, []string{"id"}, prev.IDValue, cur.IDValue)...)

	ops = append(ops, diffStringField(base, []string{"secret"}, prev.Secret, cur.Secret)...)

	return ops
}

// parseServiceStunnelServerPskConfig unmarshals VyOS ShowConfig JSON.
func parseServiceStunnelServerPskConfig(serverName, name string, data json.RawMessage) (ServiceStunnelServerPskArgs, error) {
	args := ServiceStunnelServerPskArgs{
		ServerName: serverName,
		Name:       name,
	}

	if v, ok := getNestedValue(data, []string{"id"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return ServiceStunnelServerPskArgs{}, fmt.Errorf("parse id: %w", err)
		}
		args.IDValue = &s
	}

	if v, ok := getNestedValue(data, []string{"secret"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return ServiceStunnelServerPskArgs{}, fmt.Errorf("parse secret: %w", err)
		}
		args.Secret = &s
	}

	return args, nil
}
