// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
	"regexp"
	"strconv"
)

// QoSPolicyFairQueue manages a VyOS QoSPolicyFairQueue configuration.
type QoSPolicyFairQueue struct{}

// QoSPolicyFairQueueArgs defines the inputs for the QoSPolicyFairQueue resource.
type QoSPolicyFairQueueArgs struct {
	Name         string  `pulumi:"name"`
	Description  *string `pulumi:"description,optional"`
	HashInterval *int    `pulumi:"hashInterval,optional"`
	QueueLimit   *int    `pulumi:"queueLimit,optional"`
}

// QoSPolicyFairQueueState defines the outputs for the QoSPolicyFairQueue resource.
type QoSPolicyFairQueueState struct {
	QoSPolicyFairQueueArgs
}

// Annotate provides schema metadata for the resource.
func (*QoSPolicyFairQueue) Annotate(a infer.Annotator) {
	a.Describe(new(QoSPolicyFairQueue), "Manages a VyOS Stochastic Fairness Queueing.")
}

// Annotate provides schema metadata for the inputs.
func (args *QoSPolicyFairQueueArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.Name, "Stochastic Fairness Queueing")
	a.Describe(&args.Description, "Description")
	a.Describe(&args.HashInterval, "Interval in seconds for queue algorithm perturbation")
	a.Describe(&args.QueueLimit, "Upper limit of the SFQ")
}

var qoSPolicyFairQueueNameRe = regexp.MustCompile(`^(?:` + "[[:alnum:]][-_[:alnum:]]*" + `)$`)
var qoSPolicyFairQueueDescriptionRe = regexp.MustCompile(`^(?:` + ".{0,255}" + `)$`)

// Check validates the QoSPolicyFairQueue inputs.
func (QoSPolicyFairQueue) Check(
	ctx context.Context,
	req infer.CheckRequest,
) (infer.CheckResponse[QoSPolicyFairQueueArgs], error) {
	inputs, failures, err := infer.DefaultCheck[QoSPolicyFairQueueArgs](ctx, req.NewInputs)
	if err != nil {
		return infer.CheckResponse[QoSPolicyFairQueueArgs]{}, err
	}
	if f := checkRegex(inputs.Name, qoSPolicyFairQueueNameRe, "name", "Only alpha-numeric policy name allowed"); f != nil {
		failures = append(failures, *f)
	}
	if inputs.Description != nil {
		if f := checkRegex(*inputs.Description, qoSPolicyFairQueueDescriptionRe, "description", "Description too long (limit 255 characters)"); f != nil {
			failures = append(failures, *f)
		}
	}
	if inputs.HashInterval != nil {
		if f := checkIntRange(int64(*inputs.HashInterval), 0, 2147483647, "hashInterval", "Interval must be in range 0 to 2147483647"); f != nil {
			failures = append(failures, *f)
		}
	}
	if inputs.QueueLimit != nil {
		if f := checkIntRange(int64(*inputs.QueueLimit), 1, 127, "queueLimit", "Queue limit must be in range 1 to 127"); f != nil {
			failures = append(failures, *f)
		}
	}

	return infer.CheckResponse[QoSPolicyFairQueueArgs]{Inputs: inputs, Failures: failures}, nil
}

func qoSPolicyFairQueueBasePath(name string) []any {
	return []any{"qos", "policy", "fair-queue", name}
}

// Create sets up the QoSPolicyFairQueue on VyOS.
func (QoSPolicyFairQueue) Create(
	ctx context.Context,
	req infer.CreateRequest[QoSPolicyFairQueueArgs],
) (infer.CreateResponse[QoSPolicyFairQueueState], error) {
	if req.DryRun {
		return infer.CreateResponse[QoSPolicyFairQueueState]{
			ID:     req.Inputs.Name,
			Output: QoSPolicyFairQueueState{QoSPolicyFairQueueArgs: req.Inputs},
		}, nil
	}

	ops := buildQoSPolicyFairQueueOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[QoSPolicyFairQueueState]{},
			fmt.Errorf("configure QoSPolicyFairQueue %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[QoSPolicyFairQueueState]{
		ID:     req.Inputs.Name,
		Output: QoSPolicyFairQueueState{QoSPolicyFairQueueArgs: req.Inputs},
	}, nil
}

// Read fetches the current QoSPolicyFairQueue config from VyOS.
func (QoSPolicyFairQueue) Read(
	ctx context.Context,
	req infer.ReadRequest[QoSPolicyFairQueueArgs, QoSPolicyFairQueueState],
) (infer.ReadResponse[QoSPolicyFairQueueArgs, QoSPolicyFairQueueState], error) {
	client := getClient(ctx)
	base := qoSPolicyFairQueueBasePath(req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := QoSPolicyFairQueueArgs{Name: req.State.Name}
			state := QoSPolicyFairQueueState{QoSPolicyFairQueueArgs: args}
			return infer.ReadResponse[QoSPolicyFairQueueArgs, QoSPolicyFairQueueState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[QoSPolicyFairQueueArgs, QoSPolicyFairQueueState]{},
			fmt.Errorf("read QoSPolicyFairQueue %s: %w", req.State.Name, err)
	}

	args, err := parseQoSPolicyFairQueueConfig(req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[QoSPolicyFairQueueArgs, QoSPolicyFairQueueState]{},
			fmt.Errorf("parse QoSPolicyFairQueue %s config: %w", req.State.Name, err)
	}

	state := QoSPolicyFairQueueState{QoSPolicyFairQueueArgs: args}
	return infer.ReadResponse[QoSPolicyFairQueueArgs, QoSPolicyFairQueueState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing QoSPolicyFairQueue configuration.
func (QoSPolicyFairQueue) Update(
	ctx context.Context,
	req infer.UpdateRequest[QoSPolicyFairQueueArgs, QoSPolicyFairQueueState],
) (infer.UpdateResponse[QoSPolicyFairQueueState], error) {
	if req.DryRun {
		return infer.UpdateResponse[QoSPolicyFairQueueState]{
			Output: QoSPolicyFairQueueState{QoSPolicyFairQueueArgs: req.Inputs},
		}, nil
	}

	ops := buildQoSPolicyFairQueueUpdateOps(req.State.QoSPolicyFairQueueArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[QoSPolicyFairQueueState]{},
				fmt.Errorf("update QoSPolicyFairQueue %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[QoSPolicyFairQueueState]{
		Output: QoSPolicyFairQueueState{QoSPolicyFairQueueArgs: req.Inputs},
	}, nil
}

// Delete removes the QoSPolicyFairQueue configuration from VyOS.
func (QoSPolicyFairQueue) Delete(
	ctx context.Context,
	req infer.DeleteRequest[QoSPolicyFairQueueState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := qoSPolicyFairQueueBasePath(req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete QoSPolicyFairQueue %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildQoSPolicyFairQueueOps builds set operations for all non-nil fields.
func buildQoSPolicyFairQueueOps(args QoSPolicyFairQueueArgs) []vyosclient.Operation {
	base := qoSPolicyFairQueueBasePath(args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.Description != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "description"), Value: *args.Description})
	}

	if args.HashInterval != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "hash-interval"), Value: strconv.Itoa(*args.HashInterval)})
	}

	if args.QueueLimit != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "queue-limit"), Value: strconv.Itoa(*args.QueueLimit)})
	}

	return ops
}

// buildQoSPolicyFairQueueUpdateOps computes the diff between previous and current state.
func buildQoSPolicyFairQueueUpdateOps(prev, cur QoSPolicyFairQueueArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := qoSPolicyFairQueueBasePath(cur.Name)

	ops = append(ops, diffStringField(base, []string{"description"}, prev.Description, cur.Description)...)

	ops = append(ops, diffIntField(base, []string{"hash-interval"}, prev.HashInterval, cur.HashInterval)...)

	ops = append(ops, diffIntField(base, []string{"queue-limit"}, prev.QueueLimit, cur.QueueLimit)...)

	return ops
}

// parseQoSPolicyFairQueueConfig unmarshals VyOS ShowConfig JSON.
func parseQoSPolicyFairQueueConfig(name string, data json.RawMessage) (QoSPolicyFairQueueArgs, error) {
	args := QoSPolicyFairQueueArgs{
		Name: name,
	}

	if v, ok := getNestedValue(data, []string{"description"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return QoSPolicyFairQueueArgs{}, fmt.Errorf("parse description: %w", err)
		}
		args.Description = &s
	}

	if v, ok := getNestedValue(data, []string{"hash-interval"}); ok {
		n, err := parseIntFromJSON(v)
		if err != nil {
			return QoSPolicyFairQueueArgs{}, fmt.Errorf("parse hash-interval: %w", err)
		}
		args.HashInterval = &n
	}

	if v, ok := getNestedValue(data, []string{"queue-limit"}); ok {
		n, err := parseIntFromJSON(v)
		if err != nil {
			return QoSPolicyFairQueueArgs{}, fmt.Errorf("parse queue-limit: %w", err)
		}
		args.QueueLimit = &n
	}

	return args, nil
}
