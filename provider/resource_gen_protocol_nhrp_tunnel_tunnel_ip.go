// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
)

// ProtocolNhrpTunnelTunnelIP manages a VyOS ProtocolNhrpTunnelTunnelIP configuration.
type ProtocolNhrpTunnelTunnelIP struct{}

// ProtocolNhrpTunnelTunnelIPArgs defines the inputs for the ProtocolNhrpTunnelTunnelIP resource.
type ProtocolNhrpTunnelTunnelIPArgs struct {
	TunnelName string   `pulumi:"tunnelName"`
	Name       string   `pulumi:"name"`
	Nbma       []string `pulumi:"nbma,optional"`
}

// ProtocolNhrpTunnelTunnelIPState defines the outputs for the ProtocolNhrpTunnelTunnelIP resource.
type ProtocolNhrpTunnelTunnelIPState struct {
	ProtocolNhrpTunnelTunnelIPArgs
}

// Annotate provides schema metadata for the resource.
func (*ProtocolNhrpTunnelTunnelIP) Annotate(a infer.Annotator) {
	a.Describe(new(ProtocolNhrpTunnelTunnelIP), "Manages a VyOS Set a NHRP NHS tunnel address.")
}

// Annotate provides schema metadata for the inputs.
func (args *ProtocolNhrpTunnelTunnelIPArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.TunnelName, "Parent tag node key")
	a.Describe(&args.Name, "Set a NHRP NHS tunnel address")
	a.Describe(&args.Nbma, "Set NHRP NBMA address of NHS")
}

func protocolNhrpTunnelTunnelIPBasePath(tunnelName, name string) []any {
	return []any{"protocols", "nhrp", "tunnel", tunnelName, "tunnel-ip", name}
}

// Create sets up the ProtocolNhrpTunnelTunnelIP on VyOS.
func (ProtocolNhrpTunnelTunnelIP) Create(
	ctx context.Context,
	req infer.CreateRequest[ProtocolNhrpTunnelTunnelIPArgs],
) (infer.CreateResponse[ProtocolNhrpTunnelTunnelIPState], error) {
	if req.DryRun {
		return infer.CreateResponse[ProtocolNhrpTunnelTunnelIPState]{
			ID:     req.Inputs.Name,
			Output: ProtocolNhrpTunnelTunnelIPState{ProtocolNhrpTunnelTunnelIPArgs: req.Inputs},
		}, nil
	}

	ops := buildProtocolNhrpTunnelTunnelIPOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[ProtocolNhrpTunnelTunnelIPState]{},
			fmt.Errorf("configure ProtocolNhrpTunnelTunnelIP %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[ProtocolNhrpTunnelTunnelIPState]{
		ID:     req.Inputs.Name,
		Output: ProtocolNhrpTunnelTunnelIPState{ProtocolNhrpTunnelTunnelIPArgs: req.Inputs},
	}, nil
}

// Read fetches the current ProtocolNhrpTunnelTunnelIP config from VyOS.
func (ProtocolNhrpTunnelTunnelIP) Read(
	ctx context.Context,
	req infer.ReadRequest[ProtocolNhrpTunnelTunnelIPArgs, ProtocolNhrpTunnelTunnelIPState],
) (infer.ReadResponse[ProtocolNhrpTunnelTunnelIPArgs, ProtocolNhrpTunnelTunnelIPState], error) {
	client := getClient(ctx)
	base := protocolNhrpTunnelTunnelIPBasePath(req.State.TunnelName, req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := ProtocolNhrpTunnelTunnelIPArgs{TunnelName: req.State.TunnelName, Name: req.State.Name}
			state := ProtocolNhrpTunnelTunnelIPState{ProtocolNhrpTunnelTunnelIPArgs: args}
			return infer.ReadResponse[ProtocolNhrpTunnelTunnelIPArgs, ProtocolNhrpTunnelTunnelIPState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[ProtocolNhrpTunnelTunnelIPArgs, ProtocolNhrpTunnelTunnelIPState]{},
			fmt.Errorf("read ProtocolNhrpTunnelTunnelIP %s: %w", req.State.Name, err)
	}

	args, err := parseProtocolNhrpTunnelTunnelIPConfig(req.State.TunnelName, req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[ProtocolNhrpTunnelTunnelIPArgs, ProtocolNhrpTunnelTunnelIPState]{},
			fmt.Errorf("parse ProtocolNhrpTunnelTunnelIP %s config: %w", req.State.Name, err)
	}

	state := ProtocolNhrpTunnelTunnelIPState{ProtocolNhrpTunnelTunnelIPArgs: args}
	return infer.ReadResponse[ProtocolNhrpTunnelTunnelIPArgs, ProtocolNhrpTunnelTunnelIPState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing ProtocolNhrpTunnelTunnelIP configuration.
func (ProtocolNhrpTunnelTunnelIP) Update(
	ctx context.Context,
	req infer.UpdateRequest[ProtocolNhrpTunnelTunnelIPArgs, ProtocolNhrpTunnelTunnelIPState],
) (infer.UpdateResponse[ProtocolNhrpTunnelTunnelIPState], error) {
	if req.DryRun {
		return infer.UpdateResponse[ProtocolNhrpTunnelTunnelIPState]{
			Output: ProtocolNhrpTunnelTunnelIPState{ProtocolNhrpTunnelTunnelIPArgs: req.Inputs},
		}, nil
	}

	ops := buildProtocolNhrpTunnelTunnelIPUpdateOps(req.State.ProtocolNhrpTunnelTunnelIPArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[ProtocolNhrpTunnelTunnelIPState]{},
				fmt.Errorf("update ProtocolNhrpTunnelTunnelIP %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[ProtocolNhrpTunnelTunnelIPState]{
		Output: ProtocolNhrpTunnelTunnelIPState{ProtocolNhrpTunnelTunnelIPArgs: req.Inputs},
	}, nil
}

// Delete removes the ProtocolNhrpTunnelTunnelIP configuration from VyOS.
func (ProtocolNhrpTunnelTunnelIP) Delete(
	ctx context.Context,
	req infer.DeleteRequest[ProtocolNhrpTunnelTunnelIPState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := protocolNhrpTunnelTunnelIPBasePath(req.State.TunnelName, req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete ProtocolNhrpTunnelTunnelIP %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildProtocolNhrpTunnelTunnelIPOps builds set operations for all non-nil fields.
func buildProtocolNhrpTunnelTunnelIPOps(args ProtocolNhrpTunnelTunnelIPArgs) []vyosclient.Operation {
	base := protocolNhrpTunnelTunnelIPBasePath(args.TunnelName, args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	for _, v := range args.Nbma {
		vp := make([]string, 0, 1+1)
		vp = append(vp, "nbma")
		vp = append(vp, v)
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, vp...)})
	}

	return ops
}

// buildProtocolNhrpTunnelTunnelIPUpdateOps computes the diff between previous and current state.
func buildProtocolNhrpTunnelTunnelIPUpdateOps(prev, cur ProtocolNhrpTunnelTunnelIPArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := protocolNhrpTunnelTunnelIPBasePath(cur.TunnelName, cur.Name)

	ops = append(ops, diffMultiField(base, []string{"nbma"}, prev.Nbma, cur.Nbma)...)

	return ops
}

// parseProtocolNhrpTunnelTunnelIPConfig unmarshals VyOS ShowConfig JSON.
func parseProtocolNhrpTunnelTunnelIPConfig(tunnelName, name string, data json.RawMessage) (ProtocolNhrpTunnelTunnelIPArgs, error) {
	args := ProtocolNhrpTunnelTunnelIPArgs{
		TunnelName: tunnelName,
		Name:       name,
	}

	if v, ok := getNestedValue(data, []string{"nbma"}); ok {
		vals, err := parseMultiValue(v)
		if err != nil {
			return ProtocolNhrpTunnelTunnelIPArgs{}, fmt.Errorf("parse nbma: %w", err)
		}
		args.Nbma = vals
	}

	return args, nil
}
