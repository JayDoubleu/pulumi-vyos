// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
	"strconv"
)

// VpnIpsecSiteToSitePeerTunnel manages a VyOS VpnIpsecSiteToSitePeerTunnel configuration.
type VpnIpsecSiteToSitePeerTunnel struct{}

// VpnIpsecSiteToSitePeerTunnelArgs defines the inputs for the VpnIpsecSiteToSitePeerTunnel resource.
type VpnIpsecSiteToSitePeerTunnelArgs struct {
	PeerName     string   `pulumi:"peerName"`
	Name         string   `pulumi:"name"`
	Disable      *bool    `pulumi:"disable,optional"`
	EspGroup     *string  `pulumi:"espGroup,optional"`
	Protocol     *string  `pulumi:"protocol,optional"`
	Priority     *int     `pulumi:"priority,optional"`
	LocalPort    *int     `pulumi:"localPort,optional"`
	LocalPrefix  []string `pulumi:"localPrefix,optional"`
	RemotePort   *int     `pulumi:"remotePort,optional"`
	RemotePrefix []string `pulumi:"remotePrefix,optional"`
}

// VpnIpsecSiteToSitePeerTunnelState defines the outputs for the VpnIpsecSiteToSitePeerTunnel resource.
type VpnIpsecSiteToSitePeerTunnelState struct {
	VpnIpsecSiteToSitePeerTunnelArgs
}

// Annotate provides schema metadata for the resource.
func (*VpnIpsecSiteToSitePeerTunnel) Annotate(a infer.Annotator) {
	a.Describe(new(VpnIpsecSiteToSitePeerTunnel), "Manages a VyOS Peer tunnel.")
}

// Annotate provides schema metadata for the inputs.
func (args *VpnIpsecSiteToSitePeerTunnelArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.PeerName, "Parent tag node key")
	a.Describe(&args.Name, "Peer tunnel")
	a.Describe(&args.Disable, "Disable instance")
	a.Describe(&args.EspGroup, "Encapsulating Security Payloads (ESP) group name")
	a.Describe(&args.Protocol, "Protocol")
	a.Describe(&args.Priority, "Priority for IPsec policy (lowest value more preferable)")
	a.Describe(&args.LocalPort, "Port number used by connection")
	a.Describe(&args.LocalPrefix, "Local IPv4 or IPv6 prefix")
	a.Describe(&args.RemotePort, "Port number used by connection")
	a.Describe(&args.RemotePrefix, "Remote IPv4 or IPv6 prefix")
}

// Check validates the VpnIpsecSiteToSitePeerTunnel inputs.
func (VpnIpsecSiteToSitePeerTunnel) Check(
	ctx context.Context,
	req infer.CheckRequest,
) (infer.CheckResponse[VpnIpsecSiteToSitePeerTunnelArgs], error) {
	inputs, failures, err := infer.DefaultCheck[VpnIpsecSiteToSitePeerTunnelArgs](ctx, req.NewInputs)
	if err != nil {
		return infer.CheckResponse[VpnIpsecSiteToSitePeerTunnelArgs]{}, err
	}
	if inputs.Priority != nil {
		if f := checkIntRange(int64(*inputs.Priority), 1, 100, "priority", ""); f != nil {
			failures = append(failures, *f)
		}
	}
	if inputs.LocalPort != nil {
		if f := checkIntRange(int64(*inputs.LocalPort), 1, 65535, "localPort", "Port number must be in range 1 to 65535"); f != nil {
			failures = append(failures, *f)
		}
	}
	if inputs.RemotePort != nil {
		if f := checkIntRange(int64(*inputs.RemotePort), 1, 65535, "remotePort", "Port number must be in range 1 to 65535"); f != nil {
			failures = append(failures, *f)
		}
	}

	return infer.CheckResponse[VpnIpsecSiteToSitePeerTunnelArgs]{Inputs: inputs, Failures: failures}, nil
}

func vpnIpsecSiteToSitePeerTunnelBasePath(peerName, name string) []any {
	return []any{"vpn", "ipsec", "site-to-site", "peer", peerName, "tunnel", name}
}

// Create sets up the VpnIpsecSiteToSitePeerTunnel on VyOS.
func (VpnIpsecSiteToSitePeerTunnel) Create(
	ctx context.Context,
	req infer.CreateRequest[VpnIpsecSiteToSitePeerTunnelArgs],
) (infer.CreateResponse[VpnIpsecSiteToSitePeerTunnelState], error) {
	if req.DryRun {
		return infer.CreateResponse[VpnIpsecSiteToSitePeerTunnelState]{
			ID:     req.Inputs.Name,
			Output: VpnIpsecSiteToSitePeerTunnelState{VpnIpsecSiteToSitePeerTunnelArgs: req.Inputs},
		}, nil
	}

	ops := buildVpnIpsecSiteToSitePeerTunnelOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[VpnIpsecSiteToSitePeerTunnelState]{},
			fmt.Errorf("configure VpnIpsecSiteToSitePeerTunnel %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[VpnIpsecSiteToSitePeerTunnelState]{
		ID:     req.Inputs.Name,
		Output: VpnIpsecSiteToSitePeerTunnelState{VpnIpsecSiteToSitePeerTunnelArgs: req.Inputs},
	}, nil
}

// Read fetches the current VpnIpsecSiteToSitePeerTunnel config from VyOS.
func (VpnIpsecSiteToSitePeerTunnel) Read(
	ctx context.Context,
	req infer.ReadRequest[VpnIpsecSiteToSitePeerTunnelArgs, VpnIpsecSiteToSitePeerTunnelState],
) (infer.ReadResponse[VpnIpsecSiteToSitePeerTunnelArgs, VpnIpsecSiteToSitePeerTunnelState], error) {
	client := getClient(ctx)
	base := vpnIpsecSiteToSitePeerTunnelBasePath(req.State.PeerName, req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := VpnIpsecSiteToSitePeerTunnelArgs{PeerName: req.State.PeerName, Name: req.State.Name}
			state := VpnIpsecSiteToSitePeerTunnelState{VpnIpsecSiteToSitePeerTunnelArgs: args}
			return infer.ReadResponse[VpnIpsecSiteToSitePeerTunnelArgs, VpnIpsecSiteToSitePeerTunnelState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[VpnIpsecSiteToSitePeerTunnelArgs, VpnIpsecSiteToSitePeerTunnelState]{},
			fmt.Errorf("read VpnIpsecSiteToSitePeerTunnel %s: %w", req.State.Name, err)
	}

	args, err := parseVpnIpsecSiteToSitePeerTunnelConfig(req.State.PeerName, req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[VpnIpsecSiteToSitePeerTunnelArgs, VpnIpsecSiteToSitePeerTunnelState]{},
			fmt.Errorf("parse VpnIpsecSiteToSitePeerTunnel %s config: %w", req.State.Name, err)
	}

	state := VpnIpsecSiteToSitePeerTunnelState{VpnIpsecSiteToSitePeerTunnelArgs: args}
	return infer.ReadResponse[VpnIpsecSiteToSitePeerTunnelArgs, VpnIpsecSiteToSitePeerTunnelState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing VpnIpsecSiteToSitePeerTunnel configuration.
func (VpnIpsecSiteToSitePeerTunnel) Update(
	ctx context.Context,
	req infer.UpdateRequest[VpnIpsecSiteToSitePeerTunnelArgs, VpnIpsecSiteToSitePeerTunnelState],
) (infer.UpdateResponse[VpnIpsecSiteToSitePeerTunnelState], error) {
	if req.DryRun {
		return infer.UpdateResponse[VpnIpsecSiteToSitePeerTunnelState]{
			Output: VpnIpsecSiteToSitePeerTunnelState{VpnIpsecSiteToSitePeerTunnelArgs: req.Inputs},
		}, nil
	}

	ops := buildVpnIpsecSiteToSitePeerTunnelUpdateOps(req.State.VpnIpsecSiteToSitePeerTunnelArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[VpnIpsecSiteToSitePeerTunnelState]{},
				fmt.Errorf("update VpnIpsecSiteToSitePeerTunnel %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[VpnIpsecSiteToSitePeerTunnelState]{
		Output: VpnIpsecSiteToSitePeerTunnelState{VpnIpsecSiteToSitePeerTunnelArgs: req.Inputs},
	}, nil
}

// Delete removes the VpnIpsecSiteToSitePeerTunnel configuration from VyOS.
func (VpnIpsecSiteToSitePeerTunnel) Delete(
	ctx context.Context,
	req infer.DeleteRequest[VpnIpsecSiteToSitePeerTunnelState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := vpnIpsecSiteToSitePeerTunnelBasePath(req.State.PeerName, req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete VpnIpsecSiteToSitePeerTunnel %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildVpnIpsecSiteToSitePeerTunnelOps builds set operations for all non-nil fields.
func buildVpnIpsecSiteToSitePeerTunnelOps(args VpnIpsecSiteToSitePeerTunnelArgs) []vyosclient.Operation {
	base := vpnIpsecSiteToSitePeerTunnelBasePath(args.PeerName, args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.Disable != nil && *args.Disable {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "disable")})
	}

	if args.EspGroup != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "esp-group"), Value: *args.EspGroup})
	}

	if args.Protocol != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "protocol"), Value: *args.Protocol})
	}

	if args.Priority != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "priority"), Value: strconv.Itoa(*args.Priority)})
	}

	if args.LocalPort != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "local", "port"), Value: strconv.Itoa(*args.LocalPort)})
	}

	for _, v := range args.LocalPrefix {
		vp := make([]string, 0, 2+1)
		vp = append(vp, "local")
		vp = append(vp, "prefix")
		vp = append(vp, v)
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, vp...)})
	}

	if args.RemotePort != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "remote", "port"), Value: strconv.Itoa(*args.RemotePort)})
	}

	for _, v := range args.RemotePrefix {
		vp := make([]string, 0, 2+1)
		vp = append(vp, "remote")
		vp = append(vp, "prefix")
		vp = append(vp, v)
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, vp...)})
	}

	return ops
}

// buildVpnIpsecSiteToSitePeerTunnelUpdateOps computes the diff between previous and current state.
func buildVpnIpsecSiteToSitePeerTunnelUpdateOps(prev, cur VpnIpsecSiteToSitePeerTunnelArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := vpnIpsecSiteToSitePeerTunnelBasePath(cur.PeerName, cur.Name)

	ops = append(ops, diffBoolField(base, []string{"disable"}, prev.Disable, cur.Disable)...)

	ops = append(ops, diffStringField(base, []string{"esp-group"}, prev.EspGroup, cur.EspGroup)...)

	ops = append(ops, diffStringField(base, []string{"protocol"}, prev.Protocol, cur.Protocol)...)

	ops = append(ops, diffIntField(base, []string{"priority"}, prev.Priority, cur.Priority)...)

	ops = append(ops, diffIntField(base, []string{"local", "port"}, prev.LocalPort, cur.LocalPort)...)

	ops = append(ops, diffMultiField(base, []string{"local", "prefix"}, prev.LocalPrefix, cur.LocalPrefix)...)

	ops = append(ops, diffIntField(base, []string{"remote", "port"}, prev.RemotePort, cur.RemotePort)...)

	ops = append(ops, diffMultiField(base, []string{"remote", "prefix"}, prev.RemotePrefix, cur.RemotePrefix)...)

	return ops
}

// parseVpnIpsecSiteToSitePeerTunnelConfig unmarshals VyOS ShowConfig JSON.
func parseVpnIpsecSiteToSitePeerTunnelConfig(peerName, name string, data json.RawMessage) (VpnIpsecSiteToSitePeerTunnelArgs, error) {
	args := VpnIpsecSiteToSitePeerTunnelArgs{
		PeerName: peerName,
		Name:     name,
	}

	if _, ok := getNestedValue(data, []string{"disable"}); ok {
		t := true
		args.Disable = &t
	}

	if v, ok := getNestedValue(data, []string{"esp-group"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return VpnIpsecSiteToSitePeerTunnelArgs{}, fmt.Errorf("parse esp-group: %w", err)
		}
		args.EspGroup = &s
	}

	if v, ok := getNestedValue(data, []string{"protocol"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return VpnIpsecSiteToSitePeerTunnelArgs{}, fmt.Errorf("parse protocol: %w", err)
		}
		args.Protocol = &s
	}

	if v, ok := getNestedValue(data, []string{"priority"}); ok {
		n, err := parseIntFromJSON(v)
		if err != nil {
			return VpnIpsecSiteToSitePeerTunnelArgs{}, fmt.Errorf("parse priority: %w", err)
		}
		args.Priority = &n
	}

	if v, ok := getNestedValue(data, []string{"local", "port"}); ok {
		n, err := parseIntFromJSON(v)
		if err != nil {
			return VpnIpsecSiteToSitePeerTunnelArgs{}, fmt.Errorf("parse port: %w", err)
		}
		args.LocalPort = &n
	}

	if v, ok := getNestedValue(data, []string{"local", "prefix"}); ok {
		vals, err := parseMultiValue(v)
		if err != nil {
			return VpnIpsecSiteToSitePeerTunnelArgs{}, fmt.Errorf("parse prefix: %w", err)
		}
		args.LocalPrefix = vals
	}

	if v, ok := getNestedValue(data, []string{"remote", "port"}); ok {
		n, err := parseIntFromJSON(v)
		if err != nil {
			return VpnIpsecSiteToSitePeerTunnelArgs{}, fmt.Errorf("parse port: %w", err)
		}
		args.RemotePort = &n
	}

	if v, ok := getNestedValue(data, []string{"remote", "prefix"}); ok {
		vals, err := parseMultiValue(v)
		if err != nil {
			return VpnIpsecSiteToSitePeerTunnelArgs{}, fmt.Errorf("parse prefix: %w", err)
		}
		args.RemotePrefix = vals
	}

	return args, nil
}
