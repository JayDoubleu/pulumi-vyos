// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
	"regexp"
)

// PolicyPrefixList6 manages a VyOS PolicyPrefixList6 configuration.
type PolicyPrefixList6 struct{}

// PolicyPrefixList6Args defines the inputs for the PolicyPrefixList6 resource.
type PolicyPrefixList6Args struct {
	Name        string  `pulumi:"name"`
	Description *string `pulumi:"description,optional"`
}

// PolicyPrefixList6State defines the outputs for the PolicyPrefixList6 resource.
type PolicyPrefixList6State struct {
	PolicyPrefixList6Args
}

// Annotate provides schema metadata for the resource.
func (*PolicyPrefixList6) Annotate(a infer.Annotator) {
	a.Describe(new(PolicyPrefixList6), "Manages a VyOS IPv6 prefix-list filter.")
}

// Annotate provides schema metadata for the inputs.
func (args *PolicyPrefixList6Args) Annotate(a infer.Annotator) {
	a.Describe(&args.Name, "IPv6 prefix-list filter")
	a.Describe(&args.Description, "Description")
}

var policyPrefixList6NameRe = regexp.MustCompile(`^(?:` + "[-_a-zA-Z0-9]+" + `)$`)
var policyPrefixList6DescriptionRe = regexp.MustCompile(`^(?:` + ".{0,255}" + `)$`)

// Check validates the PolicyPrefixList6 inputs.
func (PolicyPrefixList6) Check(
	ctx context.Context,
	req infer.CheckRequest,
) (infer.CheckResponse[PolicyPrefixList6Args], error) {
	inputs, failures, err := infer.DefaultCheck[PolicyPrefixList6Args](ctx, req.NewInputs)
	if err != nil {
		return infer.CheckResponse[PolicyPrefixList6Args]{}, err
	}
	if f := checkRegex(inputs.Name, policyPrefixList6NameRe, "name", "Name of prefix-list6 can only contain alpha-numeric letters, hyphen and underscores"); f != nil {
		failures = append(failures, *f)
	}
	if inputs.Description != nil {
		if f := checkRegex(*inputs.Description, policyPrefixList6DescriptionRe, "description", "Description too long (limit 255 characters)"); f != nil {
			failures = append(failures, *f)
		}
	}

	return infer.CheckResponse[PolicyPrefixList6Args]{Inputs: inputs, Failures: failures}, nil
}

func policyPrefixList6BasePath(name string) []any {
	return []any{"policy", "prefix-list6", name}
}

// Create sets up the PolicyPrefixList6 on VyOS.
func (PolicyPrefixList6) Create(
	ctx context.Context,
	req infer.CreateRequest[PolicyPrefixList6Args],
) (infer.CreateResponse[PolicyPrefixList6State], error) {
	if req.DryRun {
		return infer.CreateResponse[PolicyPrefixList6State]{
			ID:     req.Inputs.Name,
			Output: PolicyPrefixList6State{PolicyPrefixList6Args: req.Inputs},
		}, nil
	}

	ops := buildPolicyPrefixList6Ops(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[PolicyPrefixList6State]{},
			fmt.Errorf("configure PolicyPrefixList6 %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[PolicyPrefixList6State]{
		ID:     req.Inputs.Name,
		Output: PolicyPrefixList6State{PolicyPrefixList6Args: req.Inputs},
	}, nil
}

// Read fetches the current PolicyPrefixList6 config from VyOS.
func (PolicyPrefixList6) Read(
	ctx context.Context,
	req infer.ReadRequest[PolicyPrefixList6Args, PolicyPrefixList6State],
) (infer.ReadResponse[PolicyPrefixList6Args, PolicyPrefixList6State], error) {
	client := getClient(ctx)
	base := policyPrefixList6BasePath(req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := PolicyPrefixList6Args{Name: req.State.Name}
			state := PolicyPrefixList6State{PolicyPrefixList6Args: args}
			return infer.ReadResponse[PolicyPrefixList6Args, PolicyPrefixList6State]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[PolicyPrefixList6Args, PolicyPrefixList6State]{},
			fmt.Errorf("read PolicyPrefixList6 %s: %w", req.State.Name, err)
	}

	args, err := parsePolicyPrefixList6Config(req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[PolicyPrefixList6Args, PolicyPrefixList6State]{},
			fmt.Errorf("parse PolicyPrefixList6 %s config: %w", req.State.Name, err)
	}

	state := PolicyPrefixList6State{PolicyPrefixList6Args: args}
	return infer.ReadResponse[PolicyPrefixList6Args, PolicyPrefixList6State]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing PolicyPrefixList6 configuration.
func (PolicyPrefixList6) Update(
	ctx context.Context,
	req infer.UpdateRequest[PolicyPrefixList6Args, PolicyPrefixList6State],
) (infer.UpdateResponse[PolicyPrefixList6State], error) {
	if req.DryRun {
		return infer.UpdateResponse[PolicyPrefixList6State]{
			Output: PolicyPrefixList6State{PolicyPrefixList6Args: req.Inputs},
		}, nil
	}

	ops := buildPolicyPrefixList6UpdateOps(req.State.PolicyPrefixList6Args, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[PolicyPrefixList6State]{},
				fmt.Errorf("update PolicyPrefixList6 %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[PolicyPrefixList6State]{
		Output: PolicyPrefixList6State{PolicyPrefixList6Args: req.Inputs},
	}, nil
}

// Delete removes the PolicyPrefixList6 configuration from VyOS.
func (PolicyPrefixList6) Delete(
	ctx context.Context,
	req infer.DeleteRequest[PolicyPrefixList6State],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := policyPrefixList6BasePath(req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete PolicyPrefixList6 %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildPolicyPrefixList6Ops builds set operations for all non-nil fields.
func buildPolicyPrefixList6Ops(args PolicyPrefixList6Args) []vyosclient.Operation {
	base := policyPrefixList6BasePath(args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.Description != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "description"), Value: *args.Description})
	}

	return ops
}

// buildPolicyPrefixList6UpdateOps computes the diff between previous and current state.
func buildPolicyPrefixList6UpdateOps(prev, cur PolicyPrefixList6Args) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := policyPrefixList6BasePath(cur.Name)

	ops = append(ops, diffStringField(base, []string{"description"}, prev.Description, cur.Description)...)

	return ops
}

// parsePolicyPrefixList6Config unmarshals VyOS ShowConfig JSON.
func parsePolicyPrefixList6Config(name string, data json.RawMessage) (PolicyPrefixList6Args, error) {
	args := PolicyPrefixList6Args{
		Name: name,
	}

	if v, ok := getNestedValue(data, []string{"description"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return PolicyPrefixList6Args{}, fmt.Errorf("parse description: %w", err)
		}
		args.Description = &s
	}

	return args, nil
}
