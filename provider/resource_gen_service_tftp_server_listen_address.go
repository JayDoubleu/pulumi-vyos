// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
)

// ServiceTftpServerListenAddress manages a VyOS ServiceTftpServerListenAddress configuration.
type ServiceTftpServerListenAddress struct{}

// ServiceTftpServerListenAddressArgs defines the inputs for the ServiceTftpServerListenAddress resource.
type ServiceTftpServerListenAddressArgs struct {
	Name string  `pulumi:"name"`
	VRF  *string `pulumi:"vrf,optional"`
}

// ServiceTftpServerListenAddressState defines the outputs for the ServiceTftpServerListenAddress resource.
type ServiceTftpServerListenAddressState struct {
	ServiceTftpServerListenAddressArgs
}

// Annotate provides schema metadata for the resource.
func (*ServiceTftpServerListenAddress) Annotate(a infer.Annotator) {
	a.Describe(new(ServiceTftpServerListenAddress), "Manages a VyOS Local IP addresses to listen on.")
}

// Annotate provides schema metadata for the inputs.
func (args *ServiceTftpServerListenAddressArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.Name, "Local IP addresses to listen on")
	a.Describe(&args.VRF, "VRF instance name")
}

func serviceTftpServerListenAddressBasePath(name string) []any {
	return []any{"service", "tftp-server", "listen-address", name}
}

// Create sets up the ServiceTftpServerListenAddress on VyOS.
func (ServiceTftpServerListenAddress) Create(
	ctx context.Context,
	req infer.CreateRequest[ServiceTftpServerListenAddressArgs],
) (infer.CreateResponse[ServiceTftpServerListenAddressState], error) {
	if req.DryRun {
		return infer.CreateResponse[ServiceTftpServerListenAddressState]{
			ID:     req.Inputs.Name,
			Output: ServiceTftpServerListenAddressState{ServiceTftpServerListenAddressArgs: req.Inputs},
		}, nil
	}

	ops := buildServiceTftpServerListenAddressOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[ServiceTftpServerListenAddressState]{},
			fmt.Errorf("configure ServiceTftpServerListenAddress %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[ServiceTftpServerListenAddressState]{
		ID:     req.Inputs.Name,
		Output: ServiceTftpServerListenAddressState{ServiceTftpServerListenAddressArgs: req.Inputs},
	}, nil
}

// Read fetches the current ServiceTftpServerListenAddress config from VyOS.
func (ServiceTftpServerListenAddress) Read(
	ctx context.Context,
	req infer.ReadRequest[ServiceTftpServerListenAddressArgs, ServiceTftpServerListenAddressState],
) (infer.ReadResponse[ServiceTftpServerListenAddressArgs, ServiceTftpServerListenAddressState], error) {
	client := getClient(ctx)
	base := serviceTftpServerListenAddressBasePath(req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := ServiceTftpServerListenAddressArgs{Name: req.State.Name}
			state := ServiceTftpServerListenAddressState{ServiceTftpServerListenAddressArgs: args}
			return infer.ReadResponse[ServiceTftpServerListenAddressArgs, ServiceTftpServerListenAddressState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[ServiceTftpServerListenAddressArgs, ServiceTftpServerListenAddressState]{},
			fmt.Errorf("read ServiceTftpServerListenAddress %s: %w", req.State.Name, err)
	}

	args, err := parseServiceTftpServerListenAddressConfig(req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[ServiceTftpServerListenAddressArgs, ServiceTftpServerListenAddressState]{},
			fmt.Errorf("parse ServiceTftpServerListenAddress %s config: %w", req.State.Name, err)
	}

	state := ServiceTftpServerListenAddressState{ServiceTftpServerListenAddressArgs: args}
	return infer.ReadResponse[ServiceTftpServerListenAddressArgs, ServiceTftpServerListenAddressState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing ServiceTftpServerListenAddress configuration.
func (ServiceTftpServerListenAddress) Update(
	ctx context.Context,
	req infer.UpdateRequest[ServiceTftpServerListenAddressArgs, ServiceTftpServerListenAddressState],
) (infer.UpdateResponse[ServiceTftpServerListenAddressState], error) {
	if req.DryRun {
		return infer.UpdateResponse[ServiceTftpServerListenAddressState]{
			Output: ServiceTftpServerListenAddressState{ServiceTftpServerListenAddressArgs: req.Inputs},
		}, nil
	}

	ops := buildServiceTftpServerListenAddressUpdateOps(req.State.ServiceTftpServerListenAddressArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[ServiceTftpServerListenAddressState]{},
				fmt.Errorf("update ServiceTftpServerListenAddress %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[ServiceTftpServerListenAddressState]{
		Output: ServiceTftpServerListenAddressState{ServiceTftpServerListenAddressArgs: req.Inputs},
	}, nil
}

// Delete removes the ServiceTftpServerListenAddress configuration from VyOS.
func (ServiceTftpServerListenAddress) Delete(
	ctx context.Context,
	req infer.DeleteRequest[ServiceTftpServerListenAddressState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := serviceTftpServerListenAddressBasePath(req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete ServiceTftpServerListenAddress %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildServiceTftpServerListenAddressOps builds set operations for all non-nil fields.
func buildServiceTftpServerListenAddressOps(args ServiceTftpServerListenAddressArgs) []vyosclient.Operation {
	base := serviceTftpServerListenAddressBasePath(args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.VRF != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "vrf"), Value: *args.VRF})
	}

	return ops
}

// buildServiceTftpServerListenAddressUpdateOps computes the diff between previous and current state.
func buildServiceTftpServerListenAddressUpdateOps(prev, cur ServiceTftpServerListenAddressArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := serviceTftpServerListenAddressBasePath(cur.Name)

	ops = append(ops, diffStringField(base, []string{"vrf"}, prev.VRF, cur.VRF)...)

	return ops
}

// parseServiceTftpServerListenAddressConfig unmarshals VyOS ShowConfig JSON.
func parseServiceTftpServerListenAddressConfig(name string, data json.RawMessage) (ServiceTftpServerListenAddressArgs, error) {
	args := ServiceTftpServerListenAddressArgs{
		Name: name,
	}

	if v, ok := getNestedValue(data, []string{"vrf"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return ServiceTftpServerListenAddressArgs{}, fmt.Errorf("parse vrf: %w", err)
		}
		args.VRF = &s
	}

	return args, nil
}
