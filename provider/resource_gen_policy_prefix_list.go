// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
	"regexp"
)

// PolicyPrefixList manages a VyOS PolicyPrefixList configuration.
type PolicyPrefixList struct{}

// PolicyPrefixListArgs defines the inputs for the PolicyPrefixList resource.
type PolicyPrefixListArgs struct {
	Name        string  `pulumi:"name"`
	Description *string `pulumi:"description,optional"`
}

// PolicyPrefixListState defines the outputs for the PolicyPrefixList resource.
type PolicyPrefixListState struct {
	PolicyPrefixListArgs
}

// Annotate provides schema metadata for the resource.
func (*PolicyPrefixList) Annotate(a infer.Annotator) {
	a.Describe(new(PolicyPrefixList), "Manages a VyOS IP prefix-list filter.")
}

// Annotate provides schema metadata for the inputs.
func (args *PolicyPrefixListArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.Name, "IP prefix-list filter")
	a.Describe(&args.Description, "Description")
}

var policyPrefixListNameRe = regexp.MustCompile(`^(?:` + "[-_a-zA-Z0-9]+" + `)$`)
var policyPrefixListDescriptionRe = regexp.MustCompile(`^(?:` + ".{0,255}" + `)$`)

// Check validates the PolicyPrefixList inputs.
func (PolicyPrefixList) Check(
	ctx context.Context,
	req infer.CheckRequest,
) (infer.CheckResponse[PolicyPrefixListArgs], error) {
	inputs, failures, err := infer.DefaultCheck[PolicyPrefixListArgs](ctx, req.NewInputs)
	if err != nil {
		return infer.CheckResponse[PolicyPrefixListArgs]{}, err
	}
	if f := checkRegex(inputs.Name, policyPrefixListNameRe, "name", "Name of prefix-list can only contain alpha-numeric letters, hyphen and underscores"); f != nil {
		failures = append(failures, *f)
	}
	if inputs.Description != nil {
		if f := checkRegex(*inputs.Description, policyPrefixListDescriptionRe, "description", "Description too long (limit 255 characters)"); f != nil {
			failures = append(failures, *f)
		}
	}

	return infer.CheckResponse[PolicyPrefixListArgs]{Inputs: inputs, Failures: failures}, nil
}

func policyPrefixListBasePath(name string) []any {
	return []any{"policy", "prefix-list", name}
}

// Create sets up the PolicyPrefixList on VyOS.
func (PolicyPrefixList) Create(
	ctx context.Context,
	req infer.CreateRequest[PolicyPrefixListArgs],
) (infer.CreateResponse[PolicyPrefixListState], error) {
	if req.DryRun {
		return infer.CreateResponse[PolicyPrefixListState]{
			ID:     req.Inputs.Name,
			Output: PolicyPrefixListState{PolicyPrefixListArgs: req.Inputs},
		}, nil
	}

	ops := buildPolicyPrefixListOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[PolicyPrefixListState]{},
			fmt.Errorf("configure PolicyPrefixList %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[PolicyPrefixListState]{
		ID:     req.Inputs.Name,
		Output: PolicyPrefixListState{PolicyPrefixListArgs: req.Inputs},
	}, nil
}

// Read fetches the current PolicyPrefixList config from VyOS.
func (PolicyPrefixList) Read(
	ctx context.Context,
	req infer.ReadRequest[PolicyPrefixListArgs, PolicyPrefixListState],
) (infer.ReadResponse[PolicyPrefixListArgs, PolicyPrefixListState], error) {
	client := getClient(ctx)
	base := policyPrefixListBasePath(req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := PolicyPrefixListArgs{Name: req.State.Name}
			state := PolicyPrefixListState{PolicyPrefixListArgs: args}
			return infer.ReadResponse[PolicyPrefixListArgs, PolicyPrefixListState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[PolicyPrefixListArgs, PolicyPrefixListState]{},
			fmt.Errorf("read PolicyPrefixList %s: %w", req.State.Name, err)
	}

	args, err := parsePolicyPrefixListConfig(req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[PolicyPrefixListArgs, PolicyPrefixListState]{},
			fmt.Errorf("parse PolicyPrefixList %s config: %w", req.State.Name, err)
	}

	state := PolicyPrefixListState{PolicyPrefixListArgs: args}
	return infer.ReadResponse[PolicyPrefixListArgs, PolicyPrefixListState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing PolicyPrefixList configuration.
func (PolicyPrefixList) Update(
	ctx context.Context,
	req infer.UpdateRequest[PolicyPrefixListArgs, PolicyPrefixListState],
) (infer.UpdateResponse[PolicyPrefixListState], error) {
	if req.DryRun {
		return infer.UpdateResponse[PolicyPrefixListState]{
			Output: PolicyPrefixListState{PolicyPrefixListArgs: req.Inputs},
		}, nil
	}

	ops := buildPolicyPrefixListUpdateOps(req.State.PolicyPrefixListArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[PolicyPrefixListState]{},
				fmt.Errorf("update PolicyPrefixList %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[PolicyPrefixListState]{
		Output: PolicyPrefixListState{PolicyPrefixListArgs: req.Inputs},
	}, nil
}

// Delete removes the PolicyPrefixList configuration from VyOS.
func (PolicyPrefixList) Delete(
	ctx context.Context,
	req infer.DeleteRequest[PolicyPrefixListState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := policyPrefixListBasePath(req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete PolicyPrefixList %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildPolicyPrefixListOps builds set operations for all non-nil fields.
func buildPolicyPrefixListOps(args PolicyPrefixListArgs) []vyosclient.Operation {
	base := policyPrefixListBasePath(args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.Description != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "description"), Value: *args.Description})
	}

	return ops
}

// buildPolicyPrefixListUpdateOps computes the diff between previous and current state.
func buildPolicyPrefixListUpdateOps(prev, cur PolicyPrefixListArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := policyPrefixListBasePath(cur.Name)

	ops = append(ops, diffStringField(base, []string{"description"}, prev.Description, cur.Description)...)

	return ops
}

// parsePolicyPrefixListConfig unmarshals VyOS ShowConfig JSON.
func parsePolicyPrefixListConfig(name string, data json.RawMessage) (PolicyPrefixListArgs, error) {
	args := PolicyPrefixListArgs{
		Name: name,
	}

	if v, ok := getNestedValue(data, []string{"description"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return PolicyPrefixListArgs{}, fmt.Errorf("parse description: %w", err)
		}
		args.Description = &s
	}

	return args, nil
}
