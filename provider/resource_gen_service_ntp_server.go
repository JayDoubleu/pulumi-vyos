// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
)

// ServiceNTPServer manages a VyOS ServiceNTPServer configuration.
type ServiceNTPServer struct{}

// ServiceNTPServerArgs defines the inputs for the ServiceNTPServer resource.
type ServiceNTPServerArgs struct {
	Name       string `pulumi:"name"`
	Noselect   *bool  `pulumi:"noselect,optional"`
	Nts        *bool  `pulumi:"nts,optional"`
	Pool       *bool  `pulumi:"pool,optional"`
	Prefer     *bool  `pulumi:"prefer,optional"`
	Ptp        *bool  `pulumi:"ptp,optional"`
	Interleave *bool  `pulumi:"interleave,optional"`
}

// ServiceNTPServerState defines the outputs for the ServiceNTPServer resource.
type ServiceNTPServerState struct {
	ServiceNTPServerArgs
}

// Annotate provides schema metadata for the resource.
func (*ServiceNTPServer) Annotate(a infer.Annotator) {
	a.Describe(new(ServiceNTPServer), "Manages a VyOS Network Time Protocol (NTP) server.")
}

// Annotate provides schema metadata for the inputs.
func (args *ServiceNTPServerArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.Name, "Network Time Protocol (NTP) server")
	a.Describe(&args.Noselect, "Marks the server as unused")
	a.Describe(&args.Nts, "Enable Network Time Security (NTS) for the server")
	a.Describe(&args.Pool, "Associate with a number of remote servers")
	a.Describe(&args.Prefer, "Marks the server as preferred")
	a.Describe(&args.Ptp, "Use Precision Time Protocol (PTP) transport for the server")
	a.Describe(&args.Interleave, "Use the interleaved mode for the server")
}

func serviceNTPServerBasePath(name string) []any {
	return []any{"service", "ntp", "server", name}
}

// Create sets up the ServiceNTPServer on VyOS.
func (ServiceNTPServer) Create(
	ctx context.Context,
	req infer.CreateRequest[ServiceNTPServerArgs],
) (infer.CreateResponse[ServiceNTPServerState], error) {
	if req.DryRun {
		return infer.CreateResponse[ServiceNTPServerState]{
			ID:     req.Inputs.Name,
			Output: ServiceNTPServerState{ServiceNTPServerArgs: req.Inputs},
		}, nil
	}

	ops := buildServiceNTPServerOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[ServiceNTPServerState]{},
			fmt.Errorf("configure ServiceNTPServer %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[ServiceNTPServerState]{
		ID:     req.Inputs.Name,
		Output: ServiceNTPServerState{ServiceNTPServerArgs: req.Inputs},
	}, nil
}

// Read fetches the current ServiceNTPServer config from VyOS.
func (ServiceNTPServer) Read(
	ctx context.Context,
	req infer.ReadRequest[ServiceNTPServerArgs, ServiceNTPServerState],
) (infer.ReadResponse[ServiceNTPServerArgs, ServiceNTPServerState], error) {
	client := getClient(ctx)
	base := serviceNTPServerBasePath(req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := ServiceNTPServerArgs{Name: req.State.Name}
			state := ServiceNTPServerState{ServiceNTPServerArgs: args}
			return infer.ReadResponse[ServiceNTPServerArgs, ServiceNTPServerState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[ServiceNTPServerArgs, ServiceNTPServerState]{},
			fmt.Errorf("read ServiceNTPServer %s: %w", req.State.Name, err)
	}

	args, err := parseServiceNTPServerConfig(req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[ServiceNTPServerArgs, ServiceNTPServerState]{},
			fmt.Errorf("parse ServiceNTPServer %s config: %w", req.State.Name, err)
	}

	state := ServiceNTPServerState{ServiceNTPServerArgs: args}
	return infer.ReadResponse[ServiceNTPServerArgs, ServiceNTPServerState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing ServiceNTPServer configuration.
func (ServiceNTPServer) Update(
	ctx context.Context,
	req infer.UpdateRequest[ServiceNTPServerArgs, ServiceNTPServerState],
) (infer.UpdateResponse[ServiceNTPServerState], error) {
	if req.DryRun {
		return infer.UpdateResponse[ServiceNTPServerState]{
			Output: ServiceNTPServerState{ServiceNTPServerArgs: req.Inputs},
		}, nil
	}

	ops := buildServiceNTPServerUpdateOps(req.State.ServiceNTPServerArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[ServiceNTPServerState]{},
				fmt.Errorf("update ServiceNTPServer %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[ServiceNTPServerState]{
		Output: ServiceNTPServerState{ServiceNTPServerArgs: req.Inputs},
	}, nil
}

// Delete removes the ServiceNTPServer configuration from VyOS.
func (ServiceNTPServer) Delete(
	ctx context.Context,
	req infer.DeleteRequest[ServiceNTPServerState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := serviceNTPServerBasePath(req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete ServiceNTPServer %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildServiceNTPServerOps builds set operations for all non-nil fields.
func buildServiceNTPServerOps(args ServiceNTPServerArgs) []vyosclient.Operation {
	base := serviceNTPServerBasePath(args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.Noselect != nil && *args.Noselect {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "noselect")})
	}

	if args.Nts != nil && *args.Nts {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "nts")})
	}

	if args.Pool != nil && *args.Pool {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "pool")})
	}

	if args.Prefer != nil && *args.Prefer {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "prefer")})
	}

	if args.Ptp != nil && *args.Ptp {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "ptp")})
	}

	if args.Interleave != nil && *args.Interleave {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "interleave")})
	}

	return ops
}

// buildServiceNTPServerUpdateOps computes the diff between previous and current state.
func buildServiceNTPServerUpdateOps(prev, cur ServiceNTPServerArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := serviceNTPServerBasePath(cur.Name)

	ops = append(ops, diffBoolField(base, []string{"noselect"}, prev.Noselect, cur.Noselect)...)

	ops = append(ops, diffBoolField(base, []string{"nts"}, prev.Nts, cur.Nts)...)

	ops = append(ops, diffBoolField(base, []string{"pool"}, prev.Pool, cur.Pool)...)

	ops = append(ops, diffBoolField(base, []string{"prefer"}, prev.Prefer, cur.Prefer)...)

	ops = append(ops, diffBoolField(base, []string{"ptp"}, prev.Ptp, cur.Ptp)...)

	ops = append(ops, diffBoolField(base, []string{"interleave"}, prev.Interleave, cur.Interleave)...)

	return ops
}

// parseServiceNTPServerConfig unmarshals VyOS ShowConfig JSON.
func parseServiceNTPServerConfig(name string, data json.RawMessage) (ServiceNTPServerArgs, error) {
	args := ServiceNTPServerArgs{
		Name: name,
	}

	if _, ok := getNestedValue(data, []string{"noselect"}); ok {
		t := true
		args.Noselect = &t
	}

	if _, ok := getNestedValue(data, []string{"nts"}); ok {
		t := true
		args.Nts = &t
	}

	if _, ok := getNestedValue(data, []string{"pool"}); ok {
		t := true
		args.Pool = &t
	}

	if _, ok := getNestedValue(data, []string{"prefer"}); ok {
		t := true
		args.Prefer = &t
	}

	if _, ok := getNestedValue(data, []string{"ptp"}); ok {
		t := true
		args.Ptp = &t
	}

	if _, ok := getNestedValue(data, []string{"interleave"}); ok {
		t := true
		args.Interleave = &t
	}

	return args, nil
}
