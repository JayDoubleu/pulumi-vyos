// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
	"regexp"
)

// FirewallIPv4Name manages a VyOS FirewallIPv4Name configuration.
type FirewallIPv4Name struct{}

// FirewallIPv4NameArgs defines the inputs for the FirewallIPv4Name resource.
type FirewallIPv4NameArgs struct {
	Name              string  `pulumi:"name"`
	DefaultAction     *string `pulumi:"defaultAction,optional"`
	DefaultLog        *bool   `pulumi:"defaultLog,optional"`
	Description       *string `pulumi:"description,optional"`
	DefaultJumpTarget *string `pulumi:"defaultJumpTarget,optional"`
}

// FirewallIPv4NameState defines the outputs for the FirewallIPv4Name resource.
type FirewallIPv4NameState struct {
	FirewallIPv4NameArgs
}

// Annotate provides schema metadata for the resource.
func (*FirewallIPv4Name) Annotate(a infer.Annotator) {
	a.Describe(new(FirewallIPv4Name), "Manages a VyOS IPv4 custom firewall.")
}

// Annotate provides schema metadata for the inputs.
func (args *FirewallIPv4NameArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.Name, "IPv4 custom firewall")
	a.Describe(&args.DefaultAction, "Default-action for rule-set")
	a.Describe(&args.DefaultLog, "Log packets hitting default-action")
	a.Describe(&args.Description, "Description")
	a.Describe(&args.DefaultJumpTarget, "Set jump target. Action jump must be defined in default-action to use this setting")
}

var firewallIPv4NameNameRe = regexp.MustCompile(`^(?:` + "[a-zA-Z0-9][\\w\\-\\.]*" + `)$`)
var firewallIPv4NameDefaultActionRe = regexp.MustCompile(`^(?:` + "(drop|jump|reject|return|accept|continue)" + `)$`)
var firewallIPv4NameDescriptionRe = regexp.MustCompile(`^(?:` + ".{0,255}" + `)$`)

// Check validates the FirewallIPv4Name inputs.
func (FirewallIPv4Name) Check(
	ctx context.Context,
	req infer.CheckRequest,
) (infer.CheckResponse[FirewallIPv4NameArgs], error) {
	inputs, failures, err := infer.DefaultCheck[FirewallIPv4NameArgs](ctx, req.NewInputs)
	if err != nil {
		return infer.CheckResponse[FirewallIPv4NameArgs]{}, err
	}
	if f := checkRegex(inputs.Name, firewallIPv4NameNameRe, "name", ""); f != nil {
		failures = append(failures, *f)
	}
	if inputs.DefaultAction != nil {
		if f := checkRegex(*inputs.DefaultAction, firewallIPv4NameDefaultActionRe, "defaultAction", ""); f != nil {
			failures = append(failures, *f)
		}
	}
	if inputs.Description != nil {
		if f := checkRegex(*inputs.Description, firewallIPv4NameDescriptionRe, "description", "Description too long (limit 255 characters)"); f != nil {
			failures = append(failures, *f)
		}
	}

	return infer.CheckResponse[FirewallIPv4NameArgs]{Inputs: inputs, Failures: failures}, nil
}

func firewallIPv4NameBasePath(name string) []any {
	return []any{"firewall", "ipv4", "name", name}
}

// Create sets up the FirewallIPv4Name on VyOS.
func (FirewallIPv4Name) Create(
	ctx context.Context,
	req infer.CreateRequest[FirewallIPv4NameArgs],
) (infer.CreateResponse[FirewallIPv4NameState], error) {
	if req.DryRun {
		return infer.CreateResponse[FirewallIPv4NameState]{
			ID:     req.Inputs.Name,
			Output: FirewallIPv4NameState{FirewallIPv4NameArgs: req.Inputs},
		}, nil
	}

	ops := buildFirewallIPv4NameOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[FirewallIPv4NameState]{},
			fmt.Errorf("configure FirewallIPv4Name %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[FirewallIPv4NameState]{
		ID:     req.Inputs.Name,
		Output: FirewallIPv4NameState{FirewallIPv4NameArgs: req.Inputs},
	}, nil
}

// Read fetches the current FirewallIPv4Name config from VyOS.
func (FirewallIPv4Name) Read(
	ctx context.Context,
	req infer.ReadRequest[FirewallIPv4NameArgs, FirewallIPv4NameState],
) (infer.ReadResponse[FirewallIPv4NameArgs, FirewallIPv4NameState], error) {
	client := getClient(ctx)
	base := firewallIPv4NameBasePath(req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := FirewallIPv4NameArgs{Name: req.State.Name}
			state := FirewallIPv4NameState{FirewallIPv4NameArgs: args}
			return infer.ReadResponse[FirewallIPv4NameArgs, FirewallIPv4NameState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[FirewallIPv4NameArgs, FirewallIPv4NameState]{},
			fmt.Errorf("read FirewallIPv4Name %s: %w", req.State.Name, err)
	}

	args, err := parseFirewallIPv4NameConfig(req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[FirewallIPv4NameArgs, FirewallIPv4NameState]{},
			fmt.Errorf("parse FirewallIPv4Name %s config: %w", req.State.Name, err)
	}

	state := FirewallIPv4NameState{FirewallIPv4NameArgs: args}
	return infer.ReadResponse[FirewallIPv4NameArgs, FirewallIPv4NameState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing FirewallIPv4Name configuration.
func (FirewallIPv4Name) Update(
	ctx context.Context,
	req infer.UpdateRequest[FirewallIPv4NameArgs, FirewallIPv4NameState],
) (infer.UpdateResponse[FirewallIPv4NameState], error) {
	if req.DryRun {
		return infer.UpdateResponse[FirewallIPv4NameState]{
			Output: FirewallIPv4NameState{FirewallIPv4NameArgs: req.Inputs},
		}, nil
	}

	ops := buildFirewallIPv4NameUpdateOps(req.State.FirewallIPv4NameArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[FirewallIPv4NameState]{},
				fmt.Errorf("update FirewallIPv4Name %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[FirewallIPv4NameState]{
		Output: FirewallIPv4NameState{FirewallIPv4NameArgs: req.Inputs},
	}, nil
}

// Delete removes the FirewallIPv4Name configuration from VyOS.
func (FirewallIPv4Name) Delete(
	ctx context.Context,
	req infer.DeleteRequest[FirewallIPv4NameState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := firewallIPv4NameBasePath(req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete FirewallIPv4Name %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildFirewallIPv4NameOps builds set operations for all non-nil fields.
func buildFirewallIPv4NameOps(args FirewallIPv4NameArgs) []vyosclient.Operation {
	base := firewallIPv4NameBasePath(args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.DefaultAction != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "default-action"), Value: *args.DefaultAction})
	}

	if args.DefaultLog != nil && *args.DefaultLog {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "default-log")})
	}

	if args.Description != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "description"), Value: *args.Description})
	}

	if args.DefaultJumpTarget != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "default-jump-target"), Value: *args.DefaultJumpTarget})
	}

	return ops
}

// buildFirewallIPv4NameUpdateOps computes the diff between previous and current state.
func buildFirewallIPv4NameUpdateOps(prev, cur FirewallIPv4NameArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := firewallIPv4NameBasePath(cur.Name)

	ops = append(ops, diffStringField(base, []string{"default-action"}, prev.DefaultAction, cur.DefaultAction)...)

	ops = append(ops, diffBoolField(base, []string{"default-log"}, prev.DefaultLog, cur.DefaultLog)...)

	ops = append(ops, diffStringField(base, []string{"description"}, prev.Description, cur.Description)...)

	ops = append(ops, diffStringField(base, []string{"default-jump-target"}, prev.DefaultJumpTarget, cur.DefaultJumpTarget)...)

	return ops
}

// parseFirewallIPv4NameConfig unmarshals VyOS ShowConfig JSON.
func parseFirewallIPv4NameConfig(name string, data json.RawMessage) (FirewallIPv4NameArgs, error) {
	args := FirewallIPv4NameArgs{
		Name: name,
	}

	if v, ok := getNestedValue(data, []string{"default-action"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return FirewallIPv4NameArgs{}, fmt.Errorf("parse default-action: %w", err)
		}
		args.DefaultAction = &s
	}

	if _, ok := getNestedValue(data, []string{"default-log"}); ok {
		t := true
		args.DefaultLog = &t
	}

	if v, ok := getNestedValue(data, []string{"description"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return FirewallIPv4NameArgs{}, fmt.Errorf("parse description: %w", err)
		}
		args.Description = &s
	}

	if v, ok := getNestedValue(data, []string{"default-jump-target"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return FirewallIPv4NameArgs{}, fmt.Errorf("parse default-jump-target: %w", err)
		}
		args.DefaultJumpTarget = &s
	}

	return args, nil
}
