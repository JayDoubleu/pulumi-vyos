// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
	"regexp"
	"strconv"
)

// PolicyPrefixListRule manages a VyOS PolicyPrefixListRule configuration.
type PolicyPrefixListRule struct{}

// PolicyPrefixListRuleArgs defines the inputs for the PolicyPrefixListRule resource.
type PolicyPrefixListRuleArgs struct {
	PrefixListName string  `pulumi:"prefixListName"`
	Name           string  `pulumi:"name"`
	Action         *string `pulumi:"action,optional"`
	Description    *string `pulumi:"description,optional"`
	Ge             *int    `pulumi:"ge,optional"`
	Le             *int    `pulumi:"le,optional"`
	Prefix         *string `pulumi:"prefix,optional"`
}

// PolicyPrefixListRuleState defines the outputs for the PolicyPrefixListRule resource.
type PolicyPrefixListRuleState struct {
	PolicyPrefixListRuleArgs
}

// Annotate provides schema metadata for the resource.
func (*PolicyPrefixListRule) Annotate(a infer.Annotator) {
	a.Describe(new(PolicyPrefixListRule), "Manages a VyOS Rule for this prefix-list.")
}

// Annotate provides schema metadata for the inputs.
func (args *PolicyPrefixListRuleArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.PrefixListName, "Parent tag node key")
	a.Describe(&args.Name, "Rule for this prefix-list")
	a.Describe(&args.Action, "Action to take on entries matching this rule")
	a.Describe(&args.Description, "Description")
	a.Describe(&args.Ge, "Prefix length to match a netmask greater than or equal to it")
	a.Describe(&args.Le, "Prefix length to match a netmask less than or equal to it")
	a.Describe(&args.Prefix, "Prefix to match")
}

var policyPrefixListRuleActionRe = regexp.MustCompile(`^(?:` + "(permit|deny)" + `)$`)
var policyPrefixListRuleDescriptionRe = regexp.MustCompile(`^(?:` + ".{0,255}" + `)$`)

// Check validates the PolicyPrefixListRule inputs.
func (PolicyPrefixListRule) Check(
	ctx context.Context,
	req infer.CheckRequest,
) (infer.CheckResponse[PolicyPrefixListRuleArgs], error) {
	inputs, failures, err := infer.DefaultCheck[PolicyPrefixListRuleArgs](ctx, req.NewInputs)
	if err != nil {
		return infer.CheckResponse[PolicyPrefixListRuleArgs]{}, err
	}
	if inputs.Action != nil {
		if f := checkRegex(*inputs.Action, policyPrefixListRuleActionRe, "action", ""); f != nil {
			failures = append(failures, *f)
		}
	}
	if inputs.Description != nil {
		if f := checkRegex(*inputs.Description, policyPrefixListRuleDescriptionRe, "description", "Description too long (limit 255 characters)"); f != nil {
			failures = append(failures, *f)
		}
	}
	if inputs.Ge != nil {
		if f := checkIntRange(int64(*inputs.Ge), 0, 32, "ge", ""); f != nil {
			failures = append(failures, *f)
		}
	}
	if inputs.Le != nil {
		if f := checkIntRange(int64(*inputs.Le), 0, 32, "le", ""); f != nil {
			failures = append(failures, *f)
		}
	}

	return infer.CheckResponse[PolicyPrefixListRuleArgs]{Inputs: inputs, Failures: failures}, nil
}

func policyPrefixListRuleBasePath(prefixListName, name string) []any {
	return []any{"policy", "prefix-list", prefixListName, "rule", name}
}

// Create sets up the PolicyPrefixListRule on VyOS.
func (PolicyPrefixListRule) Create(
	ctx context.Context,
	req infer.CreateRequest[PolicyPrefixListRuleArgs],
) (infer.CreateResponse[PolicyPrefixListRuleState], error) {
	if req.DryRun {
		return infer.CreateResponse[PolicyPrefixListRuleState]{
			ID:     req.Inputs.Name,
			Output: PolicyPrefixListRuleState{PolicyPrefixListRuleArgs: req.Inputs},
		}, nil
	}

	ops := buildPolicyPrefixListRuleOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[PolicyPrefixListRuleState]{},
			fmt.Errorf("configure PolicyPrefixListRule %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[PolicyPrefixListRuleState]{
		ID:     req.Inputs.Name,
		Output: PolicyPrefixListRuleState{PolicyPrefixListRuleArgs: req.Inputs},
	}, nil
}

// Read fetches the current PolicyPrefixListRule config from VyOS.
func (PolicyPrefixListRule) Read(
	ctx context.Context,
	req infer.ReadRequest[PolicyPrefixListRuleArgs, PolicyPrefixListRuleState],
) (infer.ReadResponse[PolicyPrefixListRuleArgs, PolicyPrefixListRuleState], error) {
	client := getClient(ctx)
	base := policyPrefixListRuleBasePath(req.State.PrefixListName, req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := PolicyPrefixListRuleArgs{PrefixListName: req.State.PrefixListName, Name: req.State.Name}
			state := PolicyPrefixListRuleState{PolicyPrefixListRuleArgs: args}
			return infer.ReadResponse[PolicyPrefixListRuleArgs, PolicyPrefixListRuleState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[PolicyPrefixListRuleArgs, PolicyPrefixListRuleState]{},
			fmt.Errorf("read PolicyPrefixListRule %s: %w", req.State.Name, err)
	}

	args, err := parsePolicyPrefixListRuleConfig(req.State.PrefixListName, req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[PolicyPrefixListRuleArgs, PolicyPrefixListRuleState]{},
			fmt.Errorf("parse PolicyPrefixListRule %s config: %w", req.State.Name, err)
	}

	state := PolicyPrefixListRuleState{PolicyPrefixListRuleArgs: args}
	return infer.ReadResponse[PolicyPrefixListRuleArgs, PolicyPrefixListRuleState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing PolicyPrefixListRule configuration.
func (PolicyPrefixListRule) Update(
	ctx context.Context,
	req infer.UpdateRequest[PolicyPrefixListRuleArgs, PolicyPrefixListRuleState],
) (infer.UpdateResponse[PolicyPrefixListRuleState], error) {
	if req.DryRun {
		return infer.UpdateResponse[PolicyPrefixListRuleState]{
			Output: PolicyPrefixListRuleState{PolicyPrefixListRuleArgs: req.Inputs},
		}, nil
	}

	ops := buildPolicyPrefixListRuleUpdateOps(req.State.PolicyPrefixListRuleArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[PolicyPrefixListRuleState]{},
				fmt.Errorf("update PolicyPrefixListRule %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[PolicyPrefixListRuleState]{
		Output: PolicyPrefixListRuleState{PolicyPrefixListRuleArgs: req.Inputs},
	}, nil
}

// Delete removes the PolicyPrefixListRule configuration from VyOS.
func (PolicyPrefixListRule) Delete(
	ctx context.Context,
	req infer.DeleteRequest[PolicyPrefixListRuleState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := policyPrefixListRuleBasePath(req.State.PrefixListName, req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete PolicyPrefixListRule %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildPolicyPrefixListRuleOps builds set operations for all non-nil fields.
func buildPolicyPrefixListRuleOps(args PolicyPrefixListRuleArgs) []vyosclient.Operation {
	base := policyPrefixListRuleBasePath(args.PrefixListName, args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.Action != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "action"), Value: *args.Action})
	}

	if args.Description != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "description"), Value: *args.Description})
	}

	if args.Ge != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "ge"), Value: strconv.Itoa(*args.Ge)})
	}

	if args.Le != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "le"), Value: strconv.Itoa(*args.Le)})
	}

	if args.Prefix != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "prefix"), Value: *args.Prefix})
	}

	return ops
}

// buildPolicyPrefixListRuleUpdateOps computes the diff between previous and current state.
func buildPolicyPrefixListRuleUpdateOps(prev, cur PolicyPrefixListRuleArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := policyPrefixListRuleBasePath(cur.PrefixListName, cur.Name)

	ops = append(ops, diffStringField(base, []string{"action"}, prev.Action, cur.Action)...)

	ops = append(ops, diffStringField(base, []string{"description"}, prev.Description, cur.Description)...)

	ops = append(ops, diffIntField(base, []string{"ge"}, prev.Ge, cur.Ge)...)

	ops = append(ops, diffIntField(base, []string{"le"}, prev.Le, cur.Le)...)

	ops = append(ops, diffStringField(base, []string{"prefix"}, prev.Prefix, cur.Prefix)...)

	return ops
}

// parsePolicyPrefixListRuleConfig unmarshals VyOS ShowConfig JSON.
func parsePolicyPrefixListRuleConfig(prefixListName, name string, data json.RawMessage) (PolicyPrefixListRuleArgs, error) {
	args := PolicyPrefixListRuleArgs{
		PrefixListName: prefixListName,
		Name:           name,
	}

	if v, ok := getNestedValue(data, []string{"action"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return PolicyPrefixListRuleArgs{}, fmt.Errorf("parse action: %w", err)
		}
		args.Action = &s
	}

	if v, ok := getNestedValue(data, []string{"description"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return PolicyPrefixListRuleArgs{}, fmt.Errorf("parse description: %w", err)
		}
		args.Description = &s
	}

	if v, ok := getNestedValue(data, []string{"ge"}); ok {
		n, err := parseIntFromJSON(v)
		if err != nil {
			return PolicyPrefixListRuleArgs{}, fmt.Errorf("parse ge: %w", err)
		}
		args.Ge = &n
	}

	if v, ok := getNestedValue(data, []string{"le"}); ok {
		n, err := parseIntFromJSON(v)
		if err != nil {
			return PolicyPrefixListRuleArgs{}, fmt.Errorf("parse le: %w", err)
		}
		args.Le = &n
	}

	if v, ok := getNestedValue(data, []string{"prefix"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return PolicyPrefixListRuleArgs{}, fmt.Errorf("parse prefix: %w", err)
		}
		args.Prefix = &s
	}

	return args, nil
}
