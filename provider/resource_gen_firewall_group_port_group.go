// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
	"regexp"
)

// FirewallGroupPortGroup manages a VyOS FirewallGroupPortGroup configuration.
type FirewallGroupPortGroup struct{}

// FirewallGroupPortGroupArgs defines the inputs for the FirewallGroupPortGroup resource.
type FirewallGroupPortGroupArgs struct {
	Name        string   `pulumi:"name"`
	Description *string  `pulumi:"description,optional"`
	Port        []string `pulumi:"port,optional"`
	Include     []string `pulumi:"include,optional"`
}

// FirewallGroupPortGroupState defines the outputs for the FirewallGroupPortGroup resource.
type FirewallGroupPortGroupState struct {
	FirewallGroupPortGroupArgs
}

// Annotate provides schema metadata for the resource.
func (*FirewallGroupPortGroup) Annotate(a infer.Annotator) {
	a.Describe(new(FirewallGroupPortGroup), "Manages a VyOS Firewall port-group.")
}

// Annotate provides schema metadata for the inputs.
func (args *FirewallGroupPortGroupArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.Name, "Firewall port-group")
	a.Describe(&args.Description, "Description")
	a.Describe(&args.Port, "Port-group member")
	a.Describe(&args.Include, "Include another port-group")
}

var firewallGroupPortGroupNameRe = regexp.MustCompile(`^(?:` + "[-_a-zA-Z0-9][\\w\\-\\.\\+]*" + `)$`)
var firewallGroupPortGroupDescriptionRe = regexp.MustCompile(`^(?:` + ".{0,255}" + `)$`)

// Check validates the FirewallGroupPortGroup inputs.
func (FirewallGroupPortGroup) Check(
	ctx context.Context,
	req infer.CheckRequest,
) (infer.CheckResponse[FirewallGroupPortGroupArgs], error) {
	inputs, failures, err := infer.DefaultCheck[FirewallGroupPortGroupArgs](ctx, req.NewInputs)
	if err != nil {
		return infer.CheckResponse[FirewallGroupPortGroupArgs]{}, err
	}
	if f := checkRegex(inputs.Name, firewallGroupPortGroupNameRe, "name", "Name of firewall group can only contain alphanumeric letters, hyphen, underscores and dot"); f != nil {
		failures = append(failures, *f)
	}
	if inputs.Description != nil {
		if f := checkRegex(*inputs.Description, firewallGroupPortGroupDescriptionRe, "description", "Description too long (limit 255 characters)"); f != nil {
			failures = append(failures, *f)
		}
	}

	return infer.CheckResponse[FirewallGroupPortGroupArgs]{Inputs: inputs, Failures: failures}, nil
}

func firewallGroupPortGroupBasePath(name string) []any {
	return []any{"firewall", "group", "port-group", name}
}

// Create sets up the FirewallGroupPortGroup on VyOS.
func (FirewallGroupPortGroup) Create(
	ctx context.Context,
	req infer.CreateRequest[FirewallGroupPortGroupArgs],
) (infer.CreateResponse[FirewallGroupPortGroupState], error) {
	if req.DryRun {
		return infer.CreateResponse[FirewallGroupPortGroupState]{
			ID:     req.Inputs.Name,
			Output: FirewallGroupPortGroupState{FirewallGroupPortGroupArgs: req.Inputs},
		}, nil
	}

	ops := buildFirewallGroupPortGroupOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[FirewallGroupPortGroupState]{},
			fmt.Errorf("configure FirewallGroupPortGroup %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[FirewallGroupPortGroupState]{
		ID:     req.Inputs.Name,
		Output: FirewallGroupPortGroupState{FirewallGroupPortGroupArgs: req.Inputs},
	}, nil
}

// Read fetches the current FirewallGroupPortGroup config from VyOS.
func (FirewallGroupPortGroup) Read(
	ctx context.Context,
	req infer.ReadRequest[FirewallGroupPortGroupArgs, FirewallGroupPortGroupState],
) (infer.ReadResponse[FirewallGroupPortGroupArgs, FirewallGroupPortGroupState], error) {
	client := getClient(ctx)
	base := firewallGroupPortGroupBasePath(req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := FirewallGroupPortGroupArgs{Name: req.State.Name}
			state := FirewallGroupPortGroupState{FirewallGroupPortGroupArgs: args}
			return infer.ReadResponse[FirewallGroupPortGroupArgs, FirewallGroupPortGroupState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[FirewallGroupPortGroupArgs, FirewallGroupPortGroupState]{},
			fmt.Errorf("read FirewallGroupPortGroup %s: %w", req.State.Name, err)
	}

	args, err := parseFirewallGroupPortGroupConfig(req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[FirewallGroupPortGroupArgs, FirewallGroupPortGroupState]{},
			fmt.Errorf("parse FirewallGroupPortGroup %s config: %w", req.State.Name, err)
	}

	state := FirewallGroupPortGroupState{FirewallGroupPortGroupArgs: args}
	return infer.ReadResponse[FirewallGroupPortGroupArgs, FirewallGroupPortGroupState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing FirewallGroupPortGroup configuration.
func (FirewallGroupPortGroup) Update(
	ctx context.Context,
	req infer.UpdateRequest[FirewallGroupPortGroupArgs, FirewallGroupPortGroupState],
) (infer.UpdateResponse[FirewallGroupPortGroupState], error) {
	if req.DryRun {
		return infer.UpdateResponse[FirewallGroupPortGroupState]{
			Output: FirewallGroupPortGroupState{FirewallGroupPortGroupArgs: req.Inputs},
		}, nil
	}

	ops := buildFirewallGroupPortGroupUpdateOps(req.State.FirewallGroupPortGroupArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[FirewallGroupPortGroupState]{},
				fmt.Errorf("update FirewallGroupPortGroup %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[FirewallGroupPortGroupState]{
		Output: FirewallGroupPortGroupState{FirewallGroupPortGroupArgs: req.Inputs},
	}, nil
}

// Delete removes the FirewallGroupPortGroup configuration from VyOS.
func (FirewallGroupPortGroup) Delete(
	ctx context.Context,
	req infer.DeleteRequest[FirewallGroupPortGroupState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := firewallGroupPortGroupBasePath(req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete FirewallGroupPortGroup %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildFirewallGroupPortGroupOps builds set operations for all non-nil fields.
func buildFirewallGroupPortGroupOps(args FirewallGroupPortGroupArgs) []vyosclient.Operation {
	base := firewallGroupPortGroupBasePath(args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.Description != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "description"), Value: *args.Description})
	}

	for _, v := range args.Port {
		vp := make([]string, 0, 1+1)
		vp = append(vp, "port")
		vp = append(vp, v)
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, vp...)})
	}

	for _, v := range args.Include {
		vp := make([]string, 0, 1+1)
		vp = append(vp, "include")
		vp = append(vp, v)
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, vp...)})
	}

	return ops
}

// buildFirewallGroupPortGroupUpdateOps computes the diff between previous and current state.
func buildFirewallGroupPortGroupUpdateOps(prev, cur FirewallGroupPortGroupArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := firewallGroupPortGroupBasePath(cur.Name)

	ops = append(ops, diffStringField(base, []string{"description"}, prev.Description, cur.Description)...)

	ops = append(ops, diffMultiField(base, []string{"port"}, prev.Port, cur.Port)...)

	ops = append(ops, diffMultiField(base, []string{"include"}, prev.Include, cur.Include)...)

	return ops
}

// parseFirewallGroupPortGroupConfig unmarshals VyOS ShowConfig JSON.
func parseFirewallGroupPortGroupConfig(name string, data json.RawMessage) (FirewallGroupPortGroupArgs, error) {
	args := FirewallGroupPortGroupArgs{
		Name: name,
	}

	if v, ok := getNestedValue(data, []string{"description"}); ok {
		var s string
		if err := json.Unmarshal(v, &s); err != nil {
			return FirewallGroupPortGroupArgs{}, fmt.Errorf("parse description: %w", err)
		}
		args.Description = &s
	}

	if v, ok := getNestedValue(data, []string{"port"}); ok {
		vals, err := parseMultiValue(v)
		if err != nil {
			return FirewallGroupPortGroupArgs{}, fmt.Errorf("parse port: %w", err)
		}
		args.Port = vals
	}

	if v, ok := getNestedValue(data, []string{"include"}); ok {
		vals, err := parseMultiValue(v)
		if err != nil {
			return FirewallGroupPortGroupArgs{}, fmt.Errorf("parse include: %w", err)
		}
		args.Include = vals
	}

	return args, nil
}
