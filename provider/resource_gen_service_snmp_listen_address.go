// Code generated by pulumi-vyos codegen. DO NOT EDIT. Regenerate with: make generate

package provider

import (
	"context"
	"encoding/json"
	"fmt"
	"github.com/jaydoubleu/pulumi-vyos/provider/vyosclient"
	"github.com/pulumi/pulumi-go-provider/infer"
	"strconv"
)

// ServiceSnmpListenAddress manages a VyOS ServiceSnmpListenAddress configuration.
type ServiceSnmpListenAddress struct{}

// ServiceSnmpListenAddressArgs defines the inputs for the ServiceSnmpListenAddress resource.
type ServiceSnmpListenAddressArgs struct {
	Name string `pulumi:"name"`
	Port *int   `pulumi:"port,optional"`
}

// ServiceSnmpListenAddressState defines the outputs for the ServiceSnmpListenAddress resource.
type ServiceSnmpListenAddressState struct {
	ServiceSnmpListenAddressArgs
}

// Annotate provides schema metadata for the resource.
func (*ServiceSnmpListenAddress) Annotate(a infer.Annotator) {
	a.Describe(new(ServiceSnmpListenAddress), "Manages a VyOS IP address to listen for incoming SNMP requests.")
}

// Annotate provides schema metadata for the inputs.
func (args *ServiceSnmpListenAddressArgs) Annotate(a infer.Annotator) {
	a.Describe(&args.Name, "IP address to listen for incoming SNMP requests")
	a.Describe(&args.Port, "Port number used by connection")
}

// Check validates the ServiceSnmpListenAddress inputs.
func (ServiceSnmpListenAddress) Check(
	ctx context.Context,
	req infer.CheckRequest,
) (infer.CheckResponse[ServiceSnmpListenAddressArgs], error) {
	inputs, failures, err := infer.DefaultCheck[ServiceSnmpListenAddressArgs](ctx, req.NewInputs)
	if err != nil {
		return infer.CheckResponse[ServiceSnmpListenAddressArgs]{}, err
	}
	if inputs.Port != nil {
		if f := checkIntRange(int64(*inputs.Port), 1, 65535, "port", "Port number must be in range 1 to 65535"); f != nil {
			failures = append(failures, *f)
		}
	}

	return infer.CheckResponse[ServiceSnmpListenAddressArgs]{Inputs: inputs, Failures: failures}, nil
}

func serviceSnmpListenAddressBasePath(name string) []any {
	return []any{"service", "snmp", "listen-address", name}
}

// Create sets up the ServiceSnmpListenAddress on VyOS.
func (ServiceSnmpListenAddress) Create(
	ctx context.Context,
	req infer.CreateRequest[ServiceSnmpListenAddressArgs],
) (infer.CreateResponse[ServiceSnmpListenAddressState], error) {
	if req.DryRun {
		return infer.CreateResponse[ServiceSnmpListenAddressState]{
			ID:     req.Inputs.Name,
			Output: ServiceSnmpListenAddressState{ServiceSnmpListenAddressArgs: req.Inputs},
		}, nil
	}

	ops := buildServiceSnmpListenAddressOps(req.Inputs)
	client := getClient(ctx)
	if err := client.BatchConfigure(ctx, ops); err != nil {
		return infer.CreateResponse[ServiceSnmpListenAddressState]{},
			fmt.Errorf("configure ServiceSnmpListenAddress %s: %w", req.Inputs.Name, err)
	}
	saveIfEnabled(ctx)

	return infer.CreateResponse[ServiceSnmpListenAddressState]{
		ID:     req.Inputs.Name,
		Output: ServiceSnmpListenAddressState{ServiceSnmpListenAddressArgs: req.Inputs},
	}, nil
}

// Read fetches the current ServiceSnmpListenAddress config from VyOS.
func (ServiceSnmpListenAddress) Read(
	ctx context.Context,
	req infer.ReadRequest[ServiceSnmpListenAddressArgs, ServiceSnmpListenAddressState],
) (infer.ReadResponse[ServiceSnmpListenAddressArgs, ServiceSnmpListenAddressState], error) {
	client := getClient(ctx)
	base := serviceSnmpListenAddressBasePath(req.State.Name)

	data, err := client.ShowConfig(ctx, pathToStrings(base))
	if err != nil {
		if isEmptyConfigError(err) {
			// Tag node exists but has no child properties. Return minimal state.
			args := ServiceSnmpListenAddressArgs{Name: req.State.Name}
			state := ServiceSnmpListenAddressState{ServiceSnmpListenAddressArgs: args}
			return infer.ReadResponse[ServiceSnmpListenAddressArgs, ServiceSnmpListenAddressState]{
				ID:     req.State.Name,
				Inputs: args,
				State:  state,
			}, nil
		}
		return infer.ReadResponse[ServiceSnmpListenAddressArgs, ServiceSnmpListenAddressState]{},
			fmt.Errorf("read ServiceSnmpListenAddress %s: %w", req.State.Name, err)
	}

	args, err := parseServiceSnmpListenAddressConfig(req.State.Name, data)
	if err != nil {
		return infer.ReadResponse[ServiceSnmpListenAddressArgs, ServiceSnmpListenAddressState]{},
			fmt.Errorf("parse ServiceSnmpListenAddress %s config: %w", req.State.Name, err)
	}

	state := ServiceSnmpListenAddressState{ServiceSnmpListenAddressArgs: args}
	return infer.ReadResponse[ServiceSnmpListenAddressArgs, ServiceSnmpListenAddressState]{
		ID:     req.State.Name,
		Inputs: args,
		State:  state,
	}, nil
}

// Update modifies an existing ServiceSnmpListenAddress configuration.
func (ServiceSnmpListenAddress) Update(
	ctx context.Context,
	req infer.UpdateRequest[ServiceSnmpListenAddressArgs, ServiceSnmpListenAddressState],
) (infer.UpdateResponse[ServiceSnmpListenAddressState], error) {
	if req.DryRun {
		return infer.UpdateResponse[ServiceSnmpListenAddressState]{
			Output: ServiceSnmpListenAddressState{ServiceSnmpListenAddressArgs: req.Inputs},
		}, nil
	}

	ops := buildServiceSnmpListenAddressUpdateOps(req.State.ServiceSnmpListenAddressArgs, req.Inputs)
	if len(ops) > 0 {
		client := getClient(ctx)
		if err := client.BatchConfigure(ctx, ops); err != nil {
			return infer.UpdateResponse[ServiceSnmpListenAddressState]{},
				fmt.Errorf("update ServiceSnmpListenAddress %s: %w", req.Inputs.Name, err)
		}
		saveIfEnabled(ctx)
	}

	return infer.UpdateResponse[ServiceSnmpListenAddressState]{
		Output: ServiceSnmpListenAddressState{ServiceSnmpListenAddressArgs: req.Inputs},
	}, nil
}

// Delete removes the ServiceSnmpListenAddress configuration from VyOS.
func (ServiceSnmpListenAddress) Delete(
	ctx context.Context,
	req infer.DeleteRequest[ServiceSnmpListenAddressState],
) (infer.DeleteResponse, error) {
	client := getClient(ctx)
	base := serviceSnmpListenAddressBasePath(req.State.Name)
	if err := client.Delete(ctx, pathToStrings(base)); err != nil {
		return infer.DeleteResponse{}, fmt.Errorf("delete ServiceSnmpListenAddress %s: %w", req.State.Name, err)
	}
	saveIfEnabled(ctx)
	return infer.DeleteResponse{}, nil
}

// buildServiceSnmpListenAddressOps builds set operations for all non-nil fields.
func buildServiceSnmpListenAddressOps(args ServiceSnmpListenAddressArgs) []vyosclient.Operation {
	base := serviceSnmpListenAddressBasePath(args.Name)
	ops := []vyosclient.Operation{
		{Op: "set", Path: base},
	}

	if args.Port != nil {
		ops = append(ops, vyosclient.Operation{Op: "set", Path: appendPath(base, "port"), Value: strconv.Itoa(*args.Port)})
	}

	return ops
}

// buildServiceSnmpListenAddressUpdateOps computes the diff between previous and current state.
func buildServiceSnmpListenAddressUpdateOps(prev, cur ServiceSnmpListenAddressArgs) []vyosclient.Operation {
	var ops []vyosclient.Operation
	base := serviceSnmpListenAddressBasePath(cur.Name)

	ops = append(ops, diffIntField(base, []string{"port"}, prev.Port, cur.Port)...)

	return ops
}

// parseServiceSnmpListenAddressConfig unmarshals VyOS ShowConfig JSON.
func parseServiceSnmpListenAddressConfig(name string, data json.RawMessage) (ServiceSnmpListenAddressArgs, error) {
	args := ServiceSnmpListenAddressArgs{
		Name: name,
	}

	if v, ok := getNestedValue(data, []string{"port"}); ok {
		n, err := parseIntFromJSON(v)
		if err != nil {
			return ServiceSnmpListenAddressArgs{}, fmt.Errorf("parse port: %w", err)
		}
		args.Port = &n
	}

	return args, nil
}
