version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - misspell
    - revive
  exclusions:
    generated: lax
    paths:
      - sdk
      - examples
  settings:
    revive:
      rules:
        - name: exported
          arguments:
            - disableStutteringCheck
formatters:
  enable:
    - gofumpt
  exclusions:
    generated: lax
    paths:
      - sdk
      - examples
